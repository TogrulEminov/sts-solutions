<!DOCTYPE html>
<html lang="az">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Yeni Müraciət</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        box-sizing: border-box;
        padding: 0;
        margin: 0;
      }
      body {
        font-family: "Inter", sans-serif;
        background-color: #f8f9fa;
        color: #333;
        padding: 20px;
      }
      .container {
        background-color: #fff;
        max-width: 600px;
        margin: 20px auto;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 4px 12px rgba(0, 55, 81, 0.12);
      }

      /* Header */
      .header {
        background: linear-gradient(135deg, #003751 0%, #336fb3 100%);
        color: white;
        padding: 35px 25px;
        text-align: center;
        position: relative;
      }
      .header h1 {
        font-size: 26px;
        font-weight: 700;
        margin-bottom: 8px;
      }
      .header p {
        font-size: 14px;
        opacity: 0.95;
      }

      /* Badge */
      .status-badge {
        position: absolute;
        top: 15px;
        right: 15px;
        background: rgba(21, 157, 209, 0.3);
        color: white;
        padding: 6px 14px;
        width: fit-content;
        margin: 0 auto;
        border-radius: 20px;
        font-size: 11px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      /* Content */
      .content {
        padding: 30px 25px;
      }
      .intro-text {
        font-size: 15px;
        line-height: 1.6;
        margin-bottom: 25px;
        color: #575a7b;
        background: #effaff;
        padding: 15px;
        border-radius: 6px;
        border-left: 3px solid #159dd1;
      }

      /* Section */
      .section {
        margin-bottom: 25px;
      }
      .section-header {
        margin-bottom: 15px;
        padding: 12px 15px;
        background: #f4f4f4;
        border-left: 4px solid #159dd1;
        border-radius: 4px;
      }
      .section-title {
        font-size: 16px;
        font-weight: 700;
        color: #003751;
      }

      /* Table */
      .details-table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
        border: 1px solid #dee2e6;
        border-radius: 8px;
        overflow: hidden;
      }
      .details-table th,
      .details-table td {
        padding: 14px 16px;
        text-align: left;
        border-bottom: 1px solid #ececec;
        font-size: 14px;
      }
      .details-table th {
        background-color: #f9f9f9;
        font-weight: 600;
        color: #575a7b;
        width: 35%;
        font-size: 13px;
      }
      .details-table td {
        color: #003751;
        font-weight: 500;
        background: white;
      }
      .details-table tr:last-child td,
      .details-table tr:last-child th {
        border-bottom: none;
      }

      /* Message Box */
      .message-box {
        background: #effaff;
        border: 1px solid #159dd1;
        border-left: 4px solid #159dd1;
        padding: 18px;
        margin-top: 25px;
        border-radius: 6px;
      }
      .message-box strong {
        display: block;
        margin-bottom: 10px;
        color: #003751;
        font-size: 14px;
        font-weight: 700;
      }
      .message-box p {
        font-size: 14px;
        line-height: 1.6;
        color: #575a7b;
        margin: 0;
      }

      /* Footer */
      .footer {
        background-color: #f7f7f7;
        color: #575a7b;
        padding: 30px 25px;
        text-align: center;
        font-size: 13px;
        border-top: 1px solid #dee2e6;
      }
      .company-name {
        font-size: 18px;
        font-weight: 700;
        color: #003751;
        margin-bottom: 5px;
      }
      .company-tagline {
        font-size: 13px;
        color: #575a7b;
        margin-bottom: 12px;
      }
      .company-address {
        font-style: normal;
        margin: 8px 0 15px;
        color: #575a7b;
        font-size: 12px;
      }
      .divider {
        height: 1px;
        background: #dee2e6;
        margin: 15px 0;
      }
      .copyright {
        font-size: 11px;
        color: #575a7b;
        opacity: 0.8;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <!-- Header -->
      <div class="header">
        <div class="status-badge">YENİ</div>
        <h1>Yeni Müraciət Alındı</h1>
        <p><%= createdAt %></p>
      </div>

      <!-- Content -->
      <div class="content">
        <div class="intro-text">
          <strong>► Yeni müraciət alındı!</strong><br />
          Vebsaytımızdakı əlaqə forması vasitəsilə yeni bir müraciət daxil oldu.
        </div>

        <!-- Müraciət Məlumatları -->
        <div class="section">
          <div class="section-header">
            <div class="section-title">▸ Müraciət Məlumatları</div>
          </div>
          <table class="details-table">
            <tr>
              <th>Ad və Soyad</th>
              <td><%= title %></td>
            </tr>
            <tr>
              <th>Email Ünvanı</th>
              <td><%= email %></td>
            </tr>
            <tr>
              <th>Telefon Nömrəsi</th>
              <td><%= phone %></td>
            </tr>
            <tr>
              <th>Maraqlandıqı xidmət</th>
              <td><%= services %></td>
            </tr>
          </table>
        </div>

        <!-- Əlavə Mesaj -->
        <% if (message) { %>
        <div class="message-box">
          <strong>▸ Mesaj</strong>
          <p><%= message %></p>
        </div>
        <% } %>
      </div>

      <!-- Footer -->
      <div class="footer">
        <div class="company-name">Profi Transport</div>
        <div class="company-tagline">Ağır yükləriniz bizimlə yüngülləşir</div>
        <address class="company-address">Bakı, Azərbaycan</address>
        <div class="divider"></div>
        <div class="copyright">
          © <%= year %> Profi Transport. Bütün hüquqlar qorunur.
        </div>
      </div>
    </div>
  </body>
</html>
